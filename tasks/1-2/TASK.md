# 1-2：大写

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

将每个字母替换为相应的大写字母：`a`→`A`、`b`→`B`、`c`→`C`。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 3 行。

## 样例

| Input | Output |
| --- | --- |
| `abc` | `ABC` |
| `aabbcc` | `AABBCC` |
