# 3-5：首尾

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

如果输入的首个字母和最后一个字母相同，输出 `true`；否则输出 `false`。

## 限制条件

- `2 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 7 行。

## 样例

| Input | Output |
| --- | --- |
| `abc` | `false` |
| `abca` | `true` |
| `babab` | `true` |
