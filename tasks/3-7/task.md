# 3-7：回文串

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

如果输入正向和反向读起来相同，输出 `true`；否则输出 `false`。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 8 行。

## 样例

| Input | Output |
| --- | --- |
| `abba` | `true` |
| `cbabc` | `true` |
| `abca` | `false` |
