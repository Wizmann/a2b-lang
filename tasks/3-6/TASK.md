# 3-6：最多 2

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串；`a`、`b`、`c` 中恰好只有一个字母出现次数最多。

## 输出

只保留出现次数最多的字母，删除其余字母。

## 限制条件

- `1 <= 输入长度 <= 7`
- 保证只有一个字母为最多。
- 挑战目标：A=B 程序最多 11 行。

## 样例

| Input | Output |
| --- | --- |
| `cabcca` | `ccc` |
| `bbc` | `bb` |
| `a` | `a` |
