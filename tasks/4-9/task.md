# 4-9：A到B 3

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

将输入中的每个 `a` 替换为 `b`，同时将每个 `b` 替换为 `a`。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 5 行。

## 样例

| Input | Output |
| --- | --- |
| `abc` | `bac` |
| `abcabc` | `bacbac` |
