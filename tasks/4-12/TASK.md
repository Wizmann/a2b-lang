# 4-12：To B or not to B

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

如果输入包含 `b`，将每个 `a` 替换为 `b`；否则将每个 `a` 替换为 `c`。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 9 行。

## 样例

| Input | Output |
| --- | --- |
| `aabc` | `bbbc` |
| `acac` | `cccc` |
