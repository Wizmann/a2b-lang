# 4-2：移除 2

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

移除输入中最靠左的三个 `a`；如果输入少于三个 `a`，则移除全部 `a`。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 3 行。

## 样例

| Input | Output |
| --- | --- |
| `abababa` | `bbba` |
| `aaaabc` | `abc` |
