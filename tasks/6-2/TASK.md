# 6-2：回文串 2

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

若输入是回文串，输出 `true`，否则输出 `false`。

## 限制条件

- `2 <= 输入长度 <= 7`
- 不得使用 A=B 关键字；挑战目标最多 24 行。

## 样例

| Input | Output |
| --- | --- |
| `abba` | `true` |
| `cbabc` | `true` |
| `abca` | `false` |
