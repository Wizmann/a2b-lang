# 4-13：中心

## 输入

一个仅由 `a`、`b`、`c` 组成且长度为奇数的字符串。

## 输出

输出字符串最中间的字母。

## 限制条件

- `1 <= 输入长度 <= 7`，长度为奇数
- 挑战目标：A=B 程序最多 10 行。

## 样例

| Input | Output |
| --- | --- |
| `abc` | `b` |
| `ababa` | `a` |
