# 1-6：比较

## 输入

一个仅由 `a`、`b` 组成的字符串。

## 输出

输出出现次数更多的字母。

## 限制条件

- `1 <= 输入长度 <= 11`
- `a` 和 `b` 的出现次数不同。
- 挑战目标：A=B 程序最多 4 行。

## 样例

| Input | Output |
| --- | --- |
| `abb` | `b` |
| `ababa` | `a` |
