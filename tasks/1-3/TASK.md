# 1-3：去重

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

将每段连续的相同字母替换为该字母本身的单个字符。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 3 行。

## 样例

| Input | Output |
| --- | --- |
| `aabbbcc` | `abc` |
| `abccba` | `abcba` |
