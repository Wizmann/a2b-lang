# 4-4：移除 3

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

移除字符串最后三个 `a`；如果不足三个 `a`，则全部移除。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 5 行。

## 样例

| Input | Output |
| --- | --- |
| `abababa` | `abbb` |
| `aaaabc` | `abc` |
