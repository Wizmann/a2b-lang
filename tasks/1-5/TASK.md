# 1-5：排序

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

将输入字符串按照字典序 `a < b < c` 排序。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 3 行。

## 样例

| Input | Output |
| --- | --- |
| `caba` | `aabc` |
| `abccba` | `aabbcc` |
