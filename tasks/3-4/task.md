# 3-4：交换

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串；输入总是以 `a` 开始、以 `b` 结束。

## 输出

交换输入头部的连续 `a` 段和尾部的连续 `b` 段；中间部分保持不变。

## 限制条件

- `2 <= 输入长度 <= 8`
- 输入总是以 `a` 开始，以 `b` 结束。
- 挑战目标：A=B 程序最多 4 行。

## 样例

| Input | Output |
| --- | --- |
| `aacbbb` | `bbbcaa` |
| `ababab` | `bbabaa` |
