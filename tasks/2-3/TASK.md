# 2-3：恰好三个

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

如果输入长度恰好为 3，输出 `true`；否则输出 `false`。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 5 行。

## 样例

| Input | Output |
| --- | --- |
| `abc` | `true` |
| `ab` | `false` |
| `abab` | `false` |
