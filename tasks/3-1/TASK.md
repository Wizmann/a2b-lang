# 3-1：去除

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

移除输入字符串头部和尾部的所有连续 `a`。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 2 行。

## 样例

| Input | Output |
| --- | --- |
| `abcaa` | `bc` |
| `aaabbb` | `bbb` |
| `bababa` | `babab` |
