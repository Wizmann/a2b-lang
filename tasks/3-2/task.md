# 3-2：旋转

## 输入

一个仅由 `a`、`b`、`c` 组成且至少包含一个 `a` 的字符串。

## 输出

将第一个 `a` 之前的所有字符移动到字符串末尾；等价于循环左移，直到字符串以 `a` 开头。

## 限制条件

- `1 <= 输入长度 <= 7`
- 输入至少包括一个 `a`。
- 挑战目标：A=B 程序最多 2 行。

## 样例

| Input | Output |
| --- | --- |
| `cbab` | `abcb` |
| `cba` | `acb` |
| `abc` | `abc` |
