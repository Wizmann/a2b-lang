# 3-3：A到B 2

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

将输入头部和尾部的每个连续 `a` 替换为 `b`；中间部分保持不变。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 4 行。

## 样例

| Input | Output |
| --- | --- |
| `abc` | `bbc` |
| `abcba` | `bbcbb` |
| `acacaa` | `bcacbb` |
