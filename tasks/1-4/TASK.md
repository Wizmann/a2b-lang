# 1-4：去重 2

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

删除所有长度至少为 2 的连续 `a` 段；单独出现的 `a` 保留。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 2 行。

## 样例

| Input | Output |
| --- | --- |
| `aaabcaa` | `bc` |
| `baaba` | `bba` |
