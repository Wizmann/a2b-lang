# 4-16：合并

## 输入

两个仅由 `a`、`b` 组成且长度相同的字符串，以逗号（`,`）隔开。

## 输出

合并两个字符串：来自两个字符串的字母在输出中交替出现。

## 限制条件

- `1 <= 输入长度 <= 5`（两个字符串长度相同）
- 挑战目标：A=B 程序最多 9 行。

## 样例

| Input | Output |
| --- | --- |
| `aaa,bbb` | `ababab` |
| `abab,baba` | `abbaabba` |
