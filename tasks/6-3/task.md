# 6-3：To B or not to B 2

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

若输入包含 `b`，将每个 `a` 替换为 `b`；否则将每个 `a` 替换为 `c`。

## 限制条件

- `1 <= 输入长度 <= 7`
- 不得使用 A=B 关键字；挑战目标最多 13 行。

## 样例

| Input | Output |
| --- | --- |
| `aabc` | `bbbc` |
| `acac` | `cccc` |
