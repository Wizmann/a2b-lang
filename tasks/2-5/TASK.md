# 2-5：奇数

## 输入

一个仅由 `a`、`b`、`c` 组成的字符串。

## 输出

如果输入中每个字母的出现次数均为奇数或 0，输出 `true`；否则输出 `false`。

## 限制条件

- `1 <= 输入长度 <= 7`
- 挑战目标：A=B 程序最多 10 行。

## 样例

| Input | Output |
| --- | --- |
| `abc` | `true` |
| `abac` | `false` |
| `abaca` | `true` |
